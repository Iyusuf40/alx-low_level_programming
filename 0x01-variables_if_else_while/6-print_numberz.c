#include <stdio.h>
#include <stdlib.h>
/**
 * main - does
 * 
 * Return: 0 if
 */
int main() /*main*/
{
	int i;

	i = 0;

	while (i < 10)
	{
		(int)putchar(i);
		i++;
	}
	putchar('\n');
	return (0);
}
